# Hubble Environment Configuration

# Authentication Settings
# Set to false to disable authentication (development only)
AUTH_ENABLED=true

# JWT Configuration (auto-generated on first setup)
# JWT_SECRET=<auto-generated-64-char-string>
# JWT_EXPIRY=24h

# Session Configuration
# SESSION_TIMEOUT=30m

# Database Configuration
DATABASE_URL=/data/hubble.db

# Server Configuration
PORT=8889
NODE_ENV=production

# Public Domain (for share URLs)
PUBLIC_DOMAIN=hubble.blockonauts.io

# MCP Server Configuration
MCP_SERVER_URL=http://mcp-server:9900

# Docker Integration
# Set to true to enable Docker container discovery
DOCKER_DISCOVERY_ENABLED=true